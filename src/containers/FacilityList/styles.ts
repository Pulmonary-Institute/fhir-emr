import { Button } from 'antd';
import styled from 'styled-components';

export const S = {
    Rooms: styled.div`
        display: flex;
        gap: 4px;
    `,
    LinkButton: styled(Button)`
        padding: 0;
    `,
};
